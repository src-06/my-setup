sudo pacman -S --needed git base-devel blueman ffmpegthumbnailer evince
sudo pacman -S --needed --asdeps gvfs gvfs-mtp

git clone https://aur.archlinux.org/yay-bin.git ~/.cache/yay/yay-bin
cd ~/.cache/yay/yay-bin
makepkg -si

// Fish shell
yay -S --needed fish starship eza fastfetch

// Remove unwanted packages
yay -Rns htop vim

// Lightdm settings
yay -S --needed lightdm-settings

// Fonts
yay -S --needed noto-fonts-cjk inter-font ttf-jetbrains-mono-nerd ttf-ms-fonts gucharmap

// Software. zed (nodejs, npm)
yay -S --needed zen-browser-bin helium-browser-bin zed visual-studio-code-bin pear-desktop-bin yt-dlp yt-dlp-ejs github-desktop-bin localsend-bin electron30-bin antares-sql-bin kitty mpvqt fresh-editor-bin

// WPS Office
yay -S --needed wps-office ttf-wps-fonts

// Game Launcher
yay -S --needed lutris gamemode vkd3d lib32-gamemode lib32-vkd3d lib32-vulkan-icd-loader

// Niri
yay -S --needed niri xwayland-satellite qt6ct nwg-look papirus-icon-theme papirus-folders kanshi xdg-terminal-exec kanshi-menu wl-mirror
yay -S --needed --asdeps xdg-desktop-portal xdg-desktop-portal-gtk xdg-desktop-portal-gnome

// Noctalia-shell
yay -S --needed noctalia-shell cava cliphist matugen-bin wlsunset python-pywalfox
yay -D --asexplicit quickshell brightnessctl imagemagick gpu-screen-recorder cava cliphist matugen-bin wlsunset

// Developments
yay -S --needed nix composer mariadb postgresql

// Nix
// sudo systemctl enable nix-daemon
// Edit file `/etc/nix/nix.conf` add `experimental-features = nix-command flakes`

// PHP
// Edit file `/etc/php/php.ini` to extensions and activate `iconv`

// Mariadb
// sudo rm -rf /var/lib/mysql/
// sudo mariadb-install-db --user=mysql --basedir=/usr --datadir=/var/lib/mysql
// sudo systemctl start mariadb
// sudo mariadb-secure-installation
// sudo systemctl restart mariadb

// Postgres
// sudo -u postgres initdb --locale=C.UTF-8 --encoding=UTF8 -D '/var/lib/postgres/data'
// sudo systemctl start postgresql
